# v0.6 Phase 2 — Governance & Lifecycle (Approval Center + Timeline + Audit)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add Approval Center, Memory Timeline, and Audit Logs pages to the Memory Governance Console, with supporting backend APIs.

**Architecture:** 3 new backend API endpoints on the existing Hono server + 3 new frontend pages + 5 new components + 19 Storybook variants.

**Tech Stack:** Hono, Next.js 15, shadcn/ui, TanStack Query, Zustand, Storybook, Vitest

**Skill Stack:**
- `memoryos-domain` — domain rules for governance/proposal entities
- `shadcn-ui` — component generation patterns
- `frontend-design` — visual design direction

---

## Architecture Decisions (Integrated)

| Decision | Choice | Rationale |
|----------|--------|-----------|
| Real-time | **SSE** (not WebSocket) | Unidirectional server→client, simpler than WebSocket, sufficient for Phase 2 |
| Repo structure | **Monorepo** (packages/plugin, packages/console, packages/shared) | Shared types prevent frontend/backend drift |
| Build mode | **Static Export** (`output: "export"`) | No Node server needed, local-only, offline-friendly |
| Port discovery | **Configurable + well-known fallback** | `http.json` at well-known path, env var `MEMORY_SERVER_PORT` override |
| Live status | **LiveIndicator component** | Productized live/connecting/offline state, not just a dot |

## Sprint Plan

| Sprint | Focus | Components |
|--------|-------|------------|
| **Sprint 1** | Approval Center | API, ProposalCard, RiskBadge, ProposalDetail, ApprovalDiff, EvidenceList, LiveIndicator |
| **Sprint 2** | Timeline | Lifecycle API, TimelineEvent, MemoryTimeline, Diff preview |
| **Sprint 3** | Audit | Audit API, AuditTable, AuditDetailDrawer, Rollback display |

## File Structure (Monorepo)

```
opencode-memory/
├── packages/
│   ├── plugin/                  ← existing plugin code (moved from src/)
│   │   ├── src/
│   │   │   ├── server/          ← Hono HTTP server
│   │   │   │   ├── index.ts
│   │   │   │   ├── routes/
│   │   │   │   │   ├── memories.ts
│   │   │   │   │   └── governance.ts
│   │   │   │   └── middleware/
│   │   │   │       └── cors.ts
│   │   │   └── ... (existing src/)
│   │   └── package.json
│   │
│   ├── console/                 ← Next.js 15 SPA (moved from memory-console/)
│   │   ├── src/
│   │   │   ├── app/
│   │   │   │   ├── page.tsx         ← Dashboard
│   │   │   │   ├── memories/page.tsx← Explorer
│   │   │   │   ├── approval/page.tsx← Approval Center
│   │   │   │   ├── timeline/page.tsx← Timeline
│   │   │   │   └── audit/page.tsx   ← Audit Logs
│   │   │   ├── components/
│   │   │   │   ├── dashboard/
│   │   │   │   ├── memory/
│   │   │   │   ├── governance/
│   │   │   │   ├── timeline/
│   │   │   │   └── audit/
│   │   │   ├── lib/
│   │   │   └── types/
│   │   ├── design-system/
│   │   ├── stories/
│   │   └── next.config.ts
│   │
│   └── shared/                  ← Shared types between plugin + console
│       ├── src/
│       │   ├── governance.ts    ← GovernanceProposal, RiskLevel, etc.
│       │   ├── memory.ts        ← MemoryHeader, MemoryDetail
│       │   ├── audit.ts         ← AuditEvent
│       │   └── events.ts        ← SSE event types
│       └── package.json
│
├── pnpm-workspace.yaml
└── package.json
```

```
src/server/
├── routes/
│   ├── memories.ts              ← existing
│   └── governance.ts            ← NEW: proposals + approve/reject + audit
├── server/
│   └── index.ts                 ← existing, register new routes

memory-console/src/
├── app/
│   ├── page.tsx                 ← existing Dashboard
│   ├── memories/page.tsx        ← existing Explorer
│   ├── approval/page.tsx        ← NEW: Approval Center
│   ├── timeline/page.tsx        ← NEW: Memory Timeline
│   └── audit/page.tsx           ← NEW: Audit Logs
├── components/
│   ├── dashboard/               ← existing
│   ├── memory/                  ← existing
│   └── governance/
│       ├── ProposalCard.tsx     ← NEW
│       ├── RiskBadge.tsx        ← NEW
│       ├── ProposalDetail.tsx   ← NEW
│       ├── ApprovalDiff.tsx     ← NEW
│       └── EvidenceList.tsx     ← NEW
│   └── timeline/
│       ├── MemoryTimeline.tsx   ← NEW
│       ├── TimelineEvent.tsx    ← NEW
│       └── LifecycleBadge.tsx   ← NEW
│   └── audit/
│       ├── AuditTable.tsx       ← NEW
│       ├── AuditDetailDrawer.tsx← NEW
│       └── AuditFilter.tsx      ← NEW
├── types/
│   ├── api.ts                   ← existing, extend
│   └── governance.ts            ← NEW
└── stories/
    ├── MemoryCard.stories.tsx   ← existing
    ├── HealthCard.stories.tsx   ← existing
    ├── PipelineStatus.stories.tsx← existing
    ├── ProposalCard.stories.tsx ← NEW
    ├── RiskBadge.stories.tsx    ← NEW
    ├── TimelineEvent.stories.tsx← NEW
    ├── AuditRow.stories.tsx     ← NEW
    └── DiffViewer.stories.tsx   ← NEW
```

---

## Task 1: Backend — Governance + Audit API endpoints

**Files:**
- Create: `src/server/routes/governance.ts`
- Modify: `src/server/index.ts` — register new routes
- Create: `test/governance-api.test.ts`

- [ ] **Step 1: Create governance types**

Create `src/server/routes/governance.ts`:

```typescript
import { Hono } from "hono";
import { readFile, readdir } from "fs/promises";
import { join } from "path";

const app = new Hono();

// GET /api/v1/governance/proposals
app.get("/proposals", async (c) => {
  // In Phase 2, proposals are read from the RepairQueue directory
  // For now, return mock data to validate the UI
  return c.json([
    {
      id: "p001",
      type: "merge",
      targetMemory: "User prefers Python",
      reason: "Duplicate memory detected (Jaccard similarity 0.87)",
      confidence: 0.94,
      risk: "low",
      status: "pending",
      evidence: ["memory_001", "memory_002"],
      before: "Fact A: User likes Python\nFact B: User prefers Python",
      after: "Merged: User prefers Python (confidence: 0.94)",
      createdAt: new Date().toISOString(),
    },
    {
      id: "p002",
      type: "archive",
      targetMemory: "Session notes 2026-07-15",
      reason: "Session memory exceeding 30-day threshold",
      confidence: 0.82,
      risk: "low",
      status: "pending",
      evidence: ["session_20260715"],
      before: "Status: active",
      after: "Status: archived",
      createdAt: new Date(Date.now() - 3600000).toISOString(),
    },
    {
      id: "p003",
      type: "delete",
      targetMemory: "Obsolete config fact",
      reason: "Memory never recalled in 180 days, derived from obsolete session",
      confidence: 0.71,
      risk: "medium",
      status: "pending",
      evidence: ["memory_045"],
      before: "Content: use legacy API\nConfidence: inferred",
      after: "Deleted (via RepairService)",
      createdAt: new Date(Date.now() - 7200000).toISOString(),
    },
  ]);
});

// POST /api/v1/governance/proposals/:id/approve
app.post("/proposals/:id/approve", async (c) => {
  const id = c.req.param("id");
  // Placeholder: will integrate with SafeExecutionService
  return c.json({ success: true, id, status: "approved" });
});

// POST /api/v1/governance/proposals/:id/reject
app.post("/proposals/:id/reject", async (c) => {
  const id = c.req.param("id");
  return c.json({ success: true, id, status: "rejected" });
});

// GET /api/v1/audit/events
app.get("/audit/events", async (c) => {
  return c.json({
    events: [
      { id: "a001", action: "ARCHIVE", actor: "Governance", risk: "low", result: "success", timestamp: new Date().toISOString(), detail: "Archived stale session memory" },
      { id: "a002", action: "APPROVE", actor: "User", risk: "low", result: "success", timestamp: new Date(Date.now() - 600000).toISOString(), detail: "Approved merge proposal #233" },
      { id: "a003", action: "PROPOSE", actor: "Discovery", risk: "low", result: "success", timestamp: new Date(Date.now() - 1200000).toISOString(), detail: "Created merge proposal for duplicate memory" },
      { id: "a004", action: "CONFLICT", actor: "Detection", risk: "medium", result: "success", timestamp: new Date(Date.now() - 1800000).toISOString(), detail: "Detected conflict: React vs Vue preference" },
      { id: "a005", action: "MERGE", actor: "Governance", risk: "low", result: "success", timestamp: new Date(Date.now() - 3600000).toISOString(), detail: "Merged duplicate fact #1018 into #1024" },
    ],
    total: 5,
    page: 1,
    pageSize: 20,
  });
});

export { app as governanceRoutes };
```

- [ ] **Step 2: Register routes in server index**

Edit `src/server/index.ts`. Add after the `app.route("/api/v1/memories", ...)` line:

```typescript
import { governanceRoutes } from "./routes/governance.js";
// ... after the memories route registration:
app.route("/api/v1/governance", governanceRoutes);
```

- [ ] **Step 3: Create test**

Create `test/governance-api.test.ts`:

```typescript
import { describe, it, expect, beforeAll, afterAll } from "vitest";
import { mkdtemp, rm } from "fs/promises";
import { join } from "path";
import { tmpdir } from "os";
import { createStore } from "../src/store.js";
import { startMemoryServer, stopMemoryServer } from "../src/server/index.js";

describe("Governance API", () => {
  let tmpDir: string;
  let port: number;

  beforeAll(async () => {
    tmpDir = await mkdtemp(join(tmpdir(), "governance-api-test-"));
    const store = await createStore(tmpDir, { maxFiles: 200, frontmatterMaxLines: 30 });
    port = await startMemoryServer({ store, memoryDir: tmpDir, port: 0 });
  });

  afterAll(async () => {
    await stopMemoryServer();
    await rm(tmpDir, { recursive: true, force: true });
  });

  it("GET /api/v1/governance/proposals returns proposals", async () => {
    const res = await fetch(`http://127.0.0.1:${port}/api/v1/governance/proposals`);
    expect(res.status).toBe(200);
    const body = await res.json();
    expect(Array.isArray(body)).toBe(true);
    expect(body.length).toBeGreaterThan(0);
    expect(body[0]).toHaveProperty("id");
    expect(body[0]).toHaveProperty("type");
    expect(body[0]).toHaveProperty("risk");
  });

  it("POST /api/v1/governance/proposals/:id/approve approves proposal", async () => {
    const res = await fetch(`http://127.0.0.1:${port}/api/v1/governance/proposals/p001/approve`, { method: "POST" });
    expect(res.status).toBe(200);
    const body = await res.json();
    expect(body.status).toBe("approved");
  });

  it("POST /api/v1/governance/proposals/:id/reject rejects proposal", async () => {
    const res = await fetch(`http://127.0.0.1:${port}/api/v1/governance/proposals/p001/reject`, { method: "POST" });
    expect(res.status).toBe(200);
    const body = await res.json();
    expect(body.status).toBe("rejected");
  });

  it("GET /api/v1/audit/events returns audit events", async () => {
    const res = await fetch(`http://127.0.0.1:${port}/api/v1/audit/events`);
    expect(res.status).toBe(200);
    const body = await res.json();
    expect(body.events).toBeDefined();
    expect(Array.isArray(body.events)).toBe(true);
    expect(body.total).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 4: Run tests**

Run:
```bash
bun test test/governance-api.test.ts test/server.test.ts
```

Expected: 8 tests pass.

- [ ] **Step 5: Run tsc**

Run:
```bash
bunx tsc --noEmit
```

Expected: 0 errors.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat: add governance API endpoints + audit API

- GET /api/v1/governance/proposals (3 mock proposals)
- POST /api/v1/governance/proposals/:id/approve
- POST /api/v1/governance/proposals/:id/reject
- GET /api/v1/audit/events (5 mock events)
- 4 tests covering all endpoints"
```

---

## Task 2: Frontend — Governance types + API client extension

**Files:**
- Create: `memory-console/src/types/governance.ts`
- Modify: `memory-console/src/lib/api.ts` — add governance + audit fetch functions
- Modify: `memory-console/src/lib/hooks.ts` — add governance + audit hooks

- [ ] **Step 1: Create governance types**

Create `memory-console/src/types/governance.ts`:

```typescript
export type ProposalAction = "merge" | "archive" | "delete" | "split" | "resolve";
export type RiskLevel = "low" | "medium" | "high" | "critical";
export type ProposalStatus = "pending" | "approved" | "rejected" | "executed";

export interface GovernanceProposal {
  id: string;
  type: ProposalAction;
  targetMemory: string;
  reason: string;
  confidence: number;
  risk: RiskLevel;
  status: ProposalStatus;
  evidence: string[];
  before: string;
  after: string;
  createdAt: string;
}

export interface AuditEvent {
  id: string;
  action: string;
  actor: string;
  risk: RiskLevel;
  result: "success" | "failure";
  timestamp: string;
  detail: string;
}

export interface AuditResponse {
  events: AuditEvent[];
  total: number;
  page: number;
  pageSize: number;
}
```

- [ ] **Step 2: Extend API client**

Modify `memory-console/src/lib/api.ts` — add after existing functions:

```typescript
import type { GovernanceProposal, AuditResponse } from "@/types/governance";

export async function fetchProposals(): Promise<GovernanceProposal[]> {
  const res = await fetch(`${API_BASE}/governance/proposals`);
  if (!res.ok) throw new Error(`Failed to fetch proposals: ${res.status}`);
  return res.json();
}

export async function approveProposal(id: string): Promise<{ success: boolean; status: string }> {
  const res = await fetch(`${API_BASE}/governance/proposals/${encodeURIComponent(id)}/approve`, { method: "POST" });
  if (!res.ok) throw new Error(`Failed to approve proposal: ${res.status}`);
  return res.json();
}

export async function rejectProposal(id: string): Promise<{ success: boolean; status: string }> {
  const res = await fetch(`${API_BASE}/governance/proposals/${encodeURIComponent(id)}/reject`, { method: "POST" });
  if (!res.ok) throw new Error(`Failed to reject proposal: ${res.status}`);
  return res.json();
}

export async function fetchAuditEvents(params?: { page?: number }): Promise<AuditResponse> {
  const searchParams = new URLSearchParams();
  if (params?.page) searchParams.set("page", String(params.page));
  const url = `${API_BASE}/audit/events${searchParams.toString() ? `?${searchParams}` : ""}`;
  const res = await fetch(url);
  if (!res.ok) throw new Error(`Failed to fetch audit events: ${res.status}`);
  return res.json();
}
```

- [ ] **Step 3: Extend React Query hooks**

Modify `memory-console/src/lib/hooks.ts` — add after existing hooks:

```typescript
import { fetchProposals, approveProposal, rejectProposal, fetchAuditEvents } from "./api";
import { useMutation, useQueryClient } from "@tanstack/react-query";

export function useProposals() {
  return useQuery({ queryKey: ["proposals"], queryFn: fetchProposals, refetchInterval: 30_000 });
}

export function useApproveProposal() {
  const qc = useQueryClient();
  return useMutation({ mutationFn: approveProposal, onSuccess: () => qc.invalidateQueries({ queryKey: ["proposals"] }) });
}

export function useRejectProposal() {
  const qc = useQueryClient();
  return useMutation({ mutationFn: rejectProposal, onSuccess: () => qc.invalidateQueries({ queryKey: ["proposals"] }) });
}

export function useAuditEvents(params?: { page?: number }) {
  return useQuery({ queryKey: ["audit", params], queryFn: () => fetchAuditEvents(params) });
}
```

- [ ] **Step 4: Verify build**

Run:
```bash
cd D:\opencode\.config\opencode\plugins\opencode-memory\memory-console
bun run build 2>&1
```

Expected: Build succeeds.

- [ ] **Step 5: Commit**

```bash
cd D:\opencode\.config\opencode\plugins\opencode-memory
git add -f memory-console/
git commit -m "feat: add governance types + API client + React Query hooks"
```

---

## Task 3: Frontend — Governance components (ProposalCard, RiskBadge, ProposalDetail, ApprovalDiff, EvidenceList)

**Files:**
- Create: `memory-console/src/components/governance/RiskBadge.tsx`
- Create: `memory-console/src/components/governance/ProposalCard.tsx`
- Create: `memory-console/src/components/governance/EvidenceList.tsx`
- Create: `memory-console/src/components/governance/ApprovalDiff.tsx`
- Create: `memory-console/src/components/governance/ProposalDetail.tsx`

- [ ] **Step 1: Create RiskBadge**

Create `memory-console/src/components/governance/RiskBadge.tsx`:

```tsx
"use client";

import { cn } from "@/lib/utils";
import type { RiskLevel } from "@/types/governance";

const riskConfig: Record<RiskLevel, { color: string; bg: string; label: string }> = {
  low: { color: "text-green-400", bg: "bg-green-500/10 border-green-500/20", label: "LOW" },
  medium: { color: "text-amber-400", bg: "bg-amber-500/10 border-amber-500/20", label: "MEDIUM" },
  high: { color: "text-red-400", bg: "bg-red-500/10 border-red-500/20", label: "HIGH" },
  critical: { color: "text-red-500", bg: "bg-red-600/10 border-red-600/20", label: "CRITICAL" },
};

export function RiskBadge({ risk }: { risk: RiskLevel }) {
  const cfg = riskConfig[risk];
  return (
    <span className={cn("text-xs px-1.5 py-0.5 rounded border font-medium", cfg.color, cfg.bg)}>
      {cfg.label}
    </span>
  );
}
```

- [ ] **Step 2: Create ProposalCard**

Create `memory-console/src/components/governance/ProposalCard.tsx`:

```tsx
"use client";

import { cn } from "@/lib/utils";
import { RiskBadge } from "./RiskBadge";
import type { GovernanceProposal } from "@/types/governance";

const actionStyles: Record<string, string> = {
  merge: "text-blue-400",
  archive: "text-zinc-400",
  delete: "text-red-400",
  split: "text-amber-400",
  resolve: "text-green-400",
};

export function ProposalCard({ proposal, selected, onClick }: { proposal: GovernanceProposal; selected?: boolean; onClick?: () => void }) {
  return (
    <button
      onClick={onClick}
      className={cn(
        "w-full text-left px-3 py-3 rounded-md transition-colors border",
        "hover:bg-zinc-800/50",
        selected ? "bg-zinc-800 border-zinc-700" : "border-transparent",
      )}
    >
      <div className="flex items-center justify-between">
        <span className={cn("text-xs font-medium uppercase", actionStyles[proposal.type])}>
          {proposal.type}
        </span>
        <RiskBadge risk={proposal.risk} />
      </div>
      <div className="text-sm font-medium text-zinc-100 mt-1 truncate">{proposal.targetMemory}</div>
      <div className="text-xs text-zinc-500 mt-1 line-clamp-1">{proposal.reason}</div>
      <div className="flex items-center gap-2 mt-2">
        <span className="text-xs text-zinc-500">Confidence: {(proposal.confidence * 100).toFixed(0)}%</span>
        <span className="text-xs text-zinc-600">|</span>
        <span className="text-xs text-zinc-500">{proposal.evidence.length} pieces</span>
      </div>
    </button>
  );
}
```

- [ ] **Step 3: Create EvidenceList**

Create `memory-console/src/components/governance/EvidenceList.tsx`:

```tsx
"use client";

export function EvidenceList({ evidence }: { evidence: string[] }) {
  if (!evidence.length) return null;
  return (
    <div className="space-y-1">
      <h4 className="text-xs font-medium text-zinc-400 uppercase tracking-wider">Evidence</h4>
      {evidence.map((e, i) => (
        <div key={i} className="text-xs text-zinc-500 bg-zinc-900/50 rounded px-2 py-1">{e}</div>
      ))}
    </div>
  );
}
```

- [ ] **Step 4: Create ApprovalDiff**

Create `memory-console/src/components/governance/ApprovalDiff.tsx`:

```tsx
"use client";

interface ApprovalDiffProps { before: string; after: string; }

export function ApprovalDiff({ before, after }: ApprovalDiffProps) {
  return (
    <div className="space-y-2">
      <h4 className="text-xs font-medium text-zinc-400 uppercase tracking-wider">Diff</h4>
      <div className="grid grid-cols-2 gap-2">
        <div className="bg-zinc-900/50 rounded p-2 border border-red-500/20">
          <div className="text-[10px] text-red-400 uppercase mb-1">Before</div>
          <pre className="text-xs text-zinc-400 whitespace-pre-wrap font-mono">{before}</pre>
        </div>
        <div className="bg-zinc-900/50 rounded p-2 border border-green-500/20">
          <div className="text-[10px] text-green-400 uppercase mb-1">After</div>
          <pre className="text-xs text-green-300 whitespace-pre-wrap font-mono">{after}</pre>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 5: Create ProposalDetail**

Create `memory-console/src/components/governance/ProposalDetail.tsx`:

```tsx
"use client";

import { RiskBadge } from "./RiskBadge";
import { ApprovalDiff } from "./ApprovalDiff";
import { EvidenceList } from "./EvidenceList";
import { Button } from "@/components/ui/button";
import type { GovernanceProposal } from "@/types/governance";

interface ProposalDetailProps {
  proposal: GovernanceProposal | null;
  onApprove: (id: string) => void;
  onReject: (id: string) => void;
  isPending?: boolean;
}

export function ProposalDetail({ proposal, onApprove, onReject, isPending }: ProposalDetailProps) {
  if (!proposal) {
    return <div className="flex items-center justify-center h-full text-zinc-500 text-sm">Select a proposal to review</div>;
  }

  return (
    <div className="p-4 space-y-4">
      <div>
        <div className="flex items-center justify-between">
          <h2 className="text-base font-semibold text-zinc-100">{proposal.targetMemory}</h2>
          <RiskBadge risk={proposal.risk} />
        </div>
        <p className="text-sm text-zinc-400 mt-1">Proposal #{proposal.id}</p>
      </div>

      <div className="text-sm text-zinc-300 bg-zinc-900/50 rounded p-3">
        <span className="text-zinc-500">Reason: </span>{proposal.reason}
      </div>

      <ApprovalDiff before={proposal.before} after={proposal.after} />

      <EvidenceList evidence={proposal.evidence} />

      <div className="flex items-center gap-2 text-xs text-zinc-500">
        <span>Confidence: {(proposal.confidence * 100).toFixed(0)}%</span>
        <span>·</span>
        <span>Created: {new Date(proposal.createdAt).toLocaleDateString()}</span>
        <span>·</span>
        <span>Status: {proposal.status}</span>
      </div>

      {proposal.status === "pending" && (
        <div className="flex gap-2 pt-2">
          <Button onClick={() => onApprove(proposal.id)} disabled={isPending} className="bg-green-600 hover:bg-green-700 text-white">
            Approve
          </Button>
          <Button onClick={() => onReject(proposal.id)} disabled={isPending} variant="outline" className="border-red-800 text-red-400 hover:bg-red-950">
            Reject
          </Button>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 6: Verify build**

Run:
```bash
cd D:\opencode\.config\opencode\plugins\opencode-memory\memory-console
bun run build 2>&1
```

Expected: Build succeeds.

- [ ] **Step 7: Commit**

```bash
cd D:\opencode\.config\opencode\plugins\opencode-memory
git add -f memory-console/
git commit -m "feat: add governance components — ProposalCard, RiskBadge, ProposalDetail, ApprovalDiff, EvidenceList"
```

---

## Task 4: Frontend — Approval Center page

**Files:**
- Create: `memory-console/src/app/approval/page.tsx`

- [ ] **Step 1: Create Approval page**

Create `memory-console/src/app/approval/page.tsx`:

```tsx
"use client";

import { useState } from "react";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { useProposals, useApproveProposal, useRejectProposal } from "@/lib/hooks";
import { ProposalCard } from "@/components/governance/ProposalCard";
import { ProposalDetail } from "@/components/governance/ProposalDetail";
import type { GovernanceProposal } from "@/types/governance";

const queryClient = new QueryClient();

function ApprovalContent() {
  const [selectedId, setSelectedId] = useState<string | null>(null);
  const { data: proposals, isLoading } = useProposals();
  const approveMutation = useApproveProposal();
  const rejectMutation = useRejectProposal();

  const selected = proposals?.find((p: GovernanceProposal) => p.id === selectedId) ?? null;

  return (
    <div className="min-h-screen bg-black text-zinc-100">
      <header className="border-b border-zinc-800 px-6 py-4">
        <div className="flex items-center gap-4">
          <a href="/" className="text-sm text-zinc-500 hover:text-zinc-300 transition-colors">Dashboard</a>
          <span className="text-zinc-700">/</span>
          <h1 className="text-lg font-semibold">Approval Center</h1>
          {proposals && (
            <span className="text-sm text-zinc-500">
              {proposals.filter((p: GovernanceProposal) => p.status === "pending").length} pending
            </span>
          )}
        </div>
      </header>
      <div className="flex h-[calc(100vh-57px)]">
        <div className="w-72 border-r border-zinc-800 overflow-y-auto flex-shrink-0">
          {isLoading ? (
            <div className="p-4 text-sm text-zinc-500">Loading proposals...</div>
          ) : !proposals?.length ? (
            <div className="p-4 text-sm text-zinc-500">No proposals</div>
          ) : (
            <div className="p-2 space-y-1">
              {proposals.map((p: GovernanceProposal) => (
                <ProposalCard key={p.id} proposal={p} selected={selectedId === p.id} onClick={() => setSelectedId(p.id)} />
              ))}
            </div>
          )}
        </div>
        <div className="flex-1 overflow-y-auto">
          <ProposalDetail
            proposal={selected}
            onApprove={(id) => approveMutation.mutate(id)}
            onReject={(id) => rejectMutation.mutate(id)}
            isPending={approveMutation.isPending || rejectMutation.isPending}
          />
        </div>
      </div>
    </div>
  );
}

export default function ApprovalPage() {
  return (
    <QueryClientProvider client={queryClient}>
      <ApprovalContent />
    </QueryClientProvider>
  );
}
```

- [ ] **Step 2: Verify build**

Run:
```bash
cd D:\opencode\.config\opencode\plugins\opencode-memory\memory-console
bun run build 2>&1
```

Expected: Build succeeds.

- [ ] **Step 3: Commit**

```bash
cd D:\opencode\.config\opencode\plugins\opencode-memory
git add -f memory-console/
git commit -m "feat: add Approval Center page with proposal list + detail panel"
```

---

## Task 5: Frontend — Timeline components + page

**Files:**
- Create: `memory-console/src/components/timeline/LifecycleBadge.tsx`
- Create: `memory-console/src/components/timeline/TimelineEvent.tsx`
- Create: `memory-console/src/components/timeline/MemoryTimeline.tsx`
- Create: `memory-console/src/app/timeline/page.tsx`

- [ ] **Step 1: Create LifecycleBadge**

Create `memory-console/src/components/timeline/LifecycleBadge.tsx`:

```tsx
"use client";

const lifecycleColors: Record<string, string> = {
  create: "bg-blue-500/10 text-blue-400 border-blue-500/20",
  update: "bg-amber-500/10 text-amber-400 border-amber-500/20",
  merge: "bg-purple-500/10 text-purple-400 border-purple-500/20",
  archive: "bg-zinc-500/10 text-zinc-400 border-zinc-500/20",
  restore: "bg-green-500/10 text-green-400 border-green-500/20",
  active: "bg-green-500/10 text-green-400 border-green-500/20",
};

export function LifecycleBadge({ action }: { action: string }) {
  const colors = lifecycleColors[action.toLowerCase()] ?? "bg-zinc-500/10 text-zinc-400 border-zinc-500/20";
  return (
    <span className={`text-[10px] px-1.5 py-0.5 rounded border font-medium ${colors}`}>
      {action.toUpperCase()}
    </span>
  );
}
```

- [ ] **Step 2: Create TimelineEvent**

Create `memory-console/src/components/timeline/TimelineEvent.tsx`:

```tsx
"use client";

import { LifecycleBadge } from "./LifecycleBadge";

interface TimelineEventProps {
  action: string;
  timestamp: string;
  actor: string;
  detail?: string;
  isLast?: boolean;
}

export function TimelineEvent({ action, timestamp, actor, detail, isLast }: TimelineEventProps) {
  return (
    <div className="flex gap-3">
      <div className="flex flex-col items-center">
        <div className="w-3 h-3 rounded-full bg-zinc-700 border-2 border-zinc-600 flex-shrink-0 mt-1" />
        {!isLast && <div className="w-px flex-1 bg-zinc-800" />}
      </div>
      <div className="pb-6 flex-1">
        <div className="flex items-center gap-2">
          <LifecycleBadge action={action} />
          <span className="text-xs text-zinc-500">{new Date(timestamp).toLocaleDateString()}</span>
        </div>
        <div className="text-xs text-zinc-500 mt-1">Actor: {actor}</div>
        {detail && <div className="text-xs text-zinc-400 mt-1">{detail}</div>}
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Create MemoryTimeline**

Create `memory-console/src/components/timeline/MemoryTimeline.tsx`:

```tsx
"use client";

import { TimelineEvent } from "./TimelineEvent";

interface TimelineEntry {
  action: string;
  timestamp: string;
  actor: string;
  detail?: string;
}

interface MemoryTimelineProps {
  memoryName: string;
  events: TimelineEntry[];
  isActive?: boolean;
}

export function MemoryTimeline({ memoryName, events, isActive }: MemoryTimelineProps) {
  return (
    <div className="border border-zinc-800 rounded-lg p-4">
      <div className="flex items-center justify-between mb-3">
        <h3 className="text-sm font-medium text-zinc-100">{memoryName}</h3>
        {isActive && <span className="text-xs text-green-400 bg-green-500/10 px-2 py-0.5 rounded">ACTIVE</span>}
      </div>
      <div className="pl-1">
        {events.map((ev, i) => (
          <TimelineEvent key={i} {...ev} isLast={i === events.length - 1} />
        ))}
        {isActive && (
          <div className="flex gap-3">
            <div className="w-3 h-3 rounded-full bg-green-500 flex-shrink-0 mt-1" />
            <div className="text-xs text-green-400 pb-0">Current — Active</div>
          </div>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 4: Create Timeline page**

Create `memory-console/src/app/timeline/page.tsx`:

```tsx
"use client";

import { useMemories, useMemoryHistory } from "@/lib/hooks";
import { MemoryTimeline } from "@/components/timeline/MemoryTimeline";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";

const queryClient = new QueryClient();

function TimelineContent() {
  const { data: memories } = useMemories();

  return (
    <div className="min-h-screen bg-black text-zinc-100">
      <header className="border-b border-zinc-800 px-6 py-4">
        <div className="flex items-center gap-4">
          <a href="/" className="text-sm text-zinc-500 hover:text-zinc-300 transition-colors">Dashboard</a>
          <span className="text-zinc-700">/</span>
          <h1 className="text-lg font-semibold">Memory Timeline</h1>
        </div>
      </header>
      <main className="p-6">
        {!memories?.memories.length ? (
          <div className="text-sm text-zinc-500">No memories to display</div>
        ) : (
          <div className="grid grid-cols-2 gap-4">
            {memories.memories.slice(0, 20).map((m) => (
              <MemoryTimelineWrapper key={m.filename} filename={m.filename} name={m.name} status={m.status} />
            ))}
          </div>
        )}
      </main>
    </div>
  );
}

function MemoryTimelineWrapper({ filename, name, status }: { filename: string; name: string; status: string }) {
  const { data: history } = useMemoryHistory(filename);
  const events = history?.events?.length
    ? history.events.map((e: { action: string; timestamp: number; actor: string; detail?: string }) => ({
        action: e.action,
        timestamp: new Date(e.timestamp).toISOString(),
        actor: e.actor,
        detail: e.detail,
      }))
    : [{ action: "create", timestamp: new Date().toISOString(), actor: "extraction", detail: "Initial creation" }];

  return <MemoryTimeline memoryName={name || filename} events={events} isActive={status === "active"} />;
}

export default function TimelinePage() {
  return (
    <QueryClientProvider client={queryClient}>
      <TimelineContent />
    </QueryClientProvider>
  );
}
```

- [ ] **Step 5: Verify build**

Run:
```bash
cd D:\opencode\.config\opencode\plugins\opencode-memory\memory-console
bun run build 2>&1
```

Expected: Build succeeds.

- [ ] **Step 6: Commit**

```bash
cd D:\opencode\.config\opencode\plugins\opencode-memory
git add -f memory-console/
git commit -m "feat: add Memory Timeline page with lifecycle visualization"
```

---

## Task 6: Frontend — Audit Logs page

**Files:**
- Create: `memory-console/src/components/audit/AuditFilter.tsx`
- Create: `memory-console/src/components/audit/AuditTable.tsx`
- Create: `memory-console/src/components/audit/AuditDetailDrawer.tsx`
- Create: `memory-console/src/app/audit/page.tsx`

- [ ] **Step 1: Create AuditFilter**

Create `memory-console/src/components/audit/AuditFilter.tsx`:

```tsx
"use client";

import { Input } from "@/components/ui/input";

export function AuditFilter({ search, onSearchChange }: { search: string; onSearchChange: (v: string) => void }) {
  return (
    <div className="p-3 space-y-3">
      <Input placeholder="Search audit events..." value={search} onChange={(e) => onSearchChange(e.target.value)}
        className="bg-zinc-900 border-zinc-800 text-sm text-zinc-100 placeholder:text-zinc-500" />
    </div>
  );
}
```

- [ ] **Step 2: Create AuditTable**

Create `memory-console/src/components/audit/AuditTable.tsx`:

```tsx
"use client";

import { RiskBadge } from "@/components/governance/RiskBadge";
import type { AuditEvent } from "@/types/governance";

export function AuditTable({ events, onSelect }: { events: AuditEvent[]; onSelect?: (e: AuditEvent) => void }) {
  return (
    <div className="w-full">
      <div className="grid grid-cols-5 gap-4 px-4 py-2 text-xs text-zinc-500 uppercase tracking-wider border-b border-zinc-800">
        <span>Time</span>
        <span>Action</span>
        <span>Actor</span>
        <span>Risk</span>
        <span>Result</span>
      </div>
      {events.map((event) => (
        <button key={event.id} onClick={() => onSelect?.(event)}
          className="w-full grid grid-cols-5 gap-4 px-4 py-2.5 text-sm hover:bg-zinc-900/50 transition-colors border-b border-zinc-800/50 text-left">
          <span className="text-zinc-400">{new Date(event.timestamp).toLocaleTimeString()}</span>
          <span className="text-zinc-200 font-medium">{event.action}</span>
          <span className="text-zinc-400">{event.actor}</span>
          <RiskBadge risk={event.risk} />
          <span className={event.result === "success" ? "text-green-400" : "text-red-400"}>{event.result}</span>
        </button>
      ))}
    </div>
  );
}
```

- [ ] **Step 3: Create AuditDetailDrawer**

Create `memory-console/src/components/audit/AuditDetailDrawer.tsx`:

```tsx
"use client";

import { Button } from "@/components/ui/button";
import { Sheet, SheetContent, SheetHeader, SheetTitle, SheetDescription } from "@/components/ui/sheet";
import { RiskBadge } from "@/components/governance/RiskBadge";
import type { AuditEvent } from "@/types/governance";

export function AuditDetailDrawer({ event, open, onClose }: { event: AuditEvent | null; open: boolean; onClose: () => void }) {
  return (
    <Sheet open={open} onOpenChange={(o) => { if (!o) onClose(); }}>
      <SheetContent className="bg-zinc-950 border-zinc-800 text-zinc-100">
        {event && (
          <>
            <SheetHeader>
              <SheetTitle className="text-zinc-100">{event.action}</SheetTitle>
              <SheetDescription className="text-zinc-400">{event.id}</SheetDescription>
            </SheetHeader>
            <div className="mt-6 space-y-4">
              <div className="flex justify-between items-center">
                <span className="text-sm text-zinc-400">Risk</span>
                <RiskBadge risk={event.risk} />
              </div>
              <div className="flex justify-between items-center">
                <span className="text-sm text-zinc-400">Actor</span>
                <span className="text-sm text-zinc-200">{event.actor}</span>
              </div>
              <div className="flex justify-between items-center">
                <span className="text-sm text-zinc-400">Result</span>
                <span className={event.result === "success" ? "text-sm text-green-400" : "text-sm text-red-400"}>{event.result}</span>
              </div>
              <div className="flex justify-between items-center">
                <span className="text-sm text-zinc-400">Time</span>
                <span className="text-sm text-zinc-200">{new Date(event.timestamp).toLocaleString()}</span>
              </div>
              <div className="pt-3 border-t border-zinc-800">
                <span className="text-sm text-zinc-400">Detail</span>
                <p className="text-sm text-zinc-200 mt-1">{event.detail}</p>
              </div>
            </div>
            <Button variant="outline" className="w-full mt-6 border-zinc-700 text-zinc-300" onClick={onClose}>Close</Button>
          </>
        )}
      </SheetContent>
    </Sheet>
  );
}
```

- [ ] **Step 4: Create Audit page**

Create `memory-console/src/app/audit/page.tsx`:

```tsx
"use client";

import { useState } from "react";
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { useAuditEvents } from "@/lib/hooks";
import { AuditFilter } from "@/components/audit/AuditFilter";
import { AuditTable } from "@/components/audit/AuditTable";
import { AuditDetailDrawer } from "@/components/audit/AuditDetailDrawer";
import type { AuditEvent } from "@/types/governance";

const queryClient = new QueryClient();

function AuditContent() {
  const [search, setSearch] = useState("");
  const [selectedEvent, setSelectedEvent] = useState<AuditEvent | null>(null);
  const { data } = useAuditEvents();

  const events = data?.events ?? [];
  const filtered = search
    ? events.filter((e: AuditEvent) =>
        e.action.toLowerCase().includes(search.toLowerCase()) ||
        e.actor.toLowerCase().includes(search.toLowerCase()) ||
        e.detail.toLowerCase().includes(search.toLowerCase()))
    : events;

  return (
    <div className="min-h-screen bg-black text-zinc-100">
      <header className="border-b border-zinc-800 px-6 py-4">
        <div className="flex items-center gap-4">
          <a href="/" className="text-sm text-zinc-500 hover:text-zinc-300 transition-colors">Dashboard</a>
          <span className="text-zinc-700">/</span>
          <h1 className="text-lg font-semibold">Audit Logs</h1>
          <span className="text-sm text-zinc-500">{data?.total ?? 0} events</span>
        </div>
      </header>
      <div className="flex h-[calc(100vh-57px)]">
        <div className="w-56 border-r border-zinc-800 flex-shrink-0">
          <AuditFilter search={search} onSearchChange={setSearch} />
        </div>
        <div className="flex-1 overflow-y-auto">
          <AuditTable events={filtered} onSelect={setSelectedEvent} />
        </div>
      </div>
      <AuditDetailDrawer event={selectedEvent} open={!!selectedEvent} onClose={() => setSelectedEvent(null)} />
    </div>
  );
}

export default function AuditPage() {
  return (
    <QueryClientProvider client={queryClient}>
      <AuditContent />
    </QueryClientProvider>
  );
}
```

- [ ] **Step 5: Verify build**

Run:
```bash
cd D:\opencode\.config\opencode\plugins\opencode-memory\memory-console
bun run build 2>&1
```

Expected: Build succeeds.

- [ ] **Step 6: Commit**

```bash
cd D:\opencode\.config\opencode\plugins\opencode-memory
git add -f memory-console/
git commit -m "feat: add Audit Logs page with filter table + detail drawer"
```

---

## Task 7: Storybook expansion — 5 new components, 19 variants

**Files:**
- Create: `memory-console/stories/ProposalCard.stories.tsx`
- Create: `memory-console/stories/RiskBadge.stories.tsx`
- Create: `memory-console/stories/TimelineEvent.stories.tsx`
- Create: `memory-console/stories/AuditRow.stories.tsx`
- Create: `memory-console/stories/DiffViewer.stories.tsx`

- [ ] **Step 1: Create ProposalCard story**

Create `memory-console/stories/ProposalCard.stories.tsx`:

```tsx
import type { Meta, StoryObj } from "@storybook/react";
import { ProposalCard } from "../src/components/governance/ProposalCard";

const meta: Meta<typeof ProposalCard> = { title: "Governance/ProposalCard", component: ProposalCard, parameters: { layout: "centered" } };
export default meta;
type Story = StoryObj<typeof ProposalCard>;

export const MergeLow: Story = { args: { proposal: { id: "p001", type: "merge", targetMemory: "User prefers Python", reason: "Duplicate detected", confidence: 0.94, risk: "low", status: "pending", evidence: ["a", "b"], before: "", after: "", createdAt: new Date().toISOString() } } };
export const ArchiveLow: Story = { args: { proposal: { id: "p002", type: "archive", targetMemory: "Session notes", reason: "Stale session", confidence: 0.82, risk: "low", status: "pending", evidence: ["s1"], before: "", after: "", createdAt: new Date().toISOString() } } };
export const DeleteMedium: Story = { args: { proposal: { id: "p003", type: "delete", targetMemory: "Obsolete fact", reason: "Never recalled", confidence: 0.71, risk: "medium", status: "pending", evidence: ["m1"], before: "", after: "", createdAt: new Date().toISOString() } } };
export const SplitHigh: Story = { args: { proposal: { id: "p004", type: "split", targetMemory: "Large config memory", reason: "Exceeds 250 lines", confidence: 0.65, risk: "high", status: "pending", evidence: ["m1", "m2", "m3"], before: "", after: "", createdAt: new Date().toISOString() } } };
```

- [ ] **Step 2: Create RiskBadge story**

Create `memory-console/stories/RiskBadge.stories.tsx`:

```tsx
import type { Meta, StoryObj } from "@storybook/react";
import { RiskBadge } from "../src/components/governance/RiskBadge";

const meta: Meta<typeof RiskBadge> = { title: "Governance/RiskBadge", component: RiskBadge, parameters: { layout: "centered" } };
export default meta;
type Story = StoryObj<typeof RiskBadge>;

export const Low: Story = { args: { risk: "low" } };
export const Medium: Story = { args: { risk: "medium" } };
export const High: Story = { args: { risk: "high" } };
export const Critical: Story = { args: { risk: "critical" } };
```

- [ ] **Step 3: Create TimelineEvent story**

Create `memory-console/stories/TimelineEvent.stories.tsx`:

```tsx
import type { Meta, StoryObj } from "@storybook/react";
import { TimelineEvent } from "../src/components/timeline/TimelineEvent";

const meta: Meta<typeof TimelineEvent> = { title: "Timeline/TimelineEvent", component: TimelineEvent, parameters: { layout: "centered" } };
export default meta;
type Story = StoryObj<typeof TimelineEvent>;

export const Create: Story = { args: { action: "create", timestamp: new Date().toISOString(), actor: "extraction", detail: "Initial creation", isLast: false } };
export const Update: Story = { args: { action: "update", timestamp: new Date().toISOString(), actor: "user", detail: "Added FastAPI preference", isLast: false } };
export const Merge: Story = { args: { action: "merge", timestamp: new Date().toISOString(), actor: "governance", detail: "Merged duplicate #1018", isLast: false } };
export const Archive: Story = { args: { action: "archive", timestamp: new Date().toISOString(), actor: "governance", detail: "Archived stale session", isLast: false } };
export const Restore: Story = { args: { action: "restore", timestamp: new Date().toISOString(), actor: "user", detail: "Restored from archive", isLast: true } };
```

- [ ] **Step 4: Create AuditRow story**

Create `memory-console/stories/AuditRow.stories.tsx`:

```tsx
import type { Meta, StoryObj } from "@storybook/react";

const AuditRow = ({ result, risk }: { result: string; risk: string }) => (
  <div className="grid grid-cols-5 gap-4 px-4 py-2.5 text-sm border-b border-zinc-800/50 bg-black text-zinc-100">
    <span className="text-zinc-400">10:32</span>
    <span className="text-zinc-200 font-medium">ARCHIVE</span>
    <span className="text-zinc-400">Governance</span>
    <span className={`text-xs px-1.5 py-0.5 rounded border font-medium ${risk === "low" ? "text-green-400 bg-green-500/10 border-green-500/20" : "text-amber-400 bg-amber-500/10 border-amber-500/20"}`}>{risk.toUpperCase()}</span>
    <span className={result === "success" ? "text-green-400" : "text-red-400"}>{result}</span>
  </div>
);

const meta: Meta<typeof AuditRow> = { title: "Audit/AuditRow", component: AuditRow, parameters: { layout: "fullscreen" } };
export default meta;
type Story = StoryObj<typeof AuditRow>;

export const Success: Story = { args: { result: "success", risk: "low" } };
export const Failure: Story = { args: { result: "failure", risk: "medium" } };
export const Filtered: Story = { args: { result: "success", risk: "high" } };
```

- [ ] **Step 5: Create DiffViewer story**

Create `memory-console/stories/DiffViewer.stories.tsx`:

```tsx
import type { Meta, StoryObj } from "@storybook/react";
import { ApprovalDiff } from "../src/components/governance/ApprovalDiff";

const meta: Meta<typeof ApprovalDiff> = { title: "Governance/ApprovalDiff", component: ApprovalDiff, parameters: { layout: "centered" } };
export default meta;
type Story = StoryObj<typeof ApprovalDiff>;

export const Merge: Story = { args: { before: "Fact A: User likes Python\nFact B: User prefers Python", after: "Merged: User prefers Python (confidence: 0.94)" } };
export const Conflict: Story = { args: { before: "Claim A: I use React (2025)\nClaim B: I use Vue (2026)", after: "Resolution: Newer evidence wins (Vue, 2026)" } };
export const Archive: Story = { args: { before: "Status: active\nRecall count: 0", after: "Status: archived\nReason: 180 days untouched" } };
```

- [ ] **Step 6: Verify Storybook build**

Run:
```bash
cd D:\opencode\.config\opencode\plugins\opencode-memory\memory-console
bunx storybook build --quiet 2>&1
```

Expected: Storybook builds with 8 components, 26+ variants.

- [ ] **Step 7: Commit**

```bash
cd D:\opencode\.config\opencode\plugins\opencode-memory
git add -f memory-console/
git commit -m "feat: add Storybook stories for 5 governance/timeline/audit components"
```

---

## Task 8: End-to-end verification

- [ ] **Step 1: Run all plugin tests**

```bash
cd D:\opencode\.config\opencode\plugins\opencode-memory
bun test test/server.test.ts test/governance-api.test.ts
```

Expected: 8 tests pass.

- [ ] **Step 2: Verify tsc**

```bash
bunx tsc --noEmit
```

Expected: 0 errors.

- [ ] **Step 3: Verify Next.js build**

```bash
cd D:\opencode\.config\opencode\plugins\opencode-memory\memory-console
bun run build
```

Expected: 6 pages, 0 errors.

- [ ] **Step 4: Final push**

```bash
cd D:\opencode\.config\opencode\plugins\opencode-memory
git push origin main
```

---

## Spec Coverage Check

| Spec Section | Task | Status |
|-------------|------|--------|
| 12.2 Backend API Extensions | Task 1 | ✅ 4 endpoints |
| 12.3 Approval Center | Task 3-4 | ✅ ProposalCard, RiskBadge, ProposalDetail, page |
| 12.4 Memory Timeline | Task 5 | ✅ TimelineEvent, MemoryTimeline, page |
| 12.5 Audit Logs | Task 6 | ✅ AuditTable, AuditDetailDrawer, page |
| 12.6 Storybook Expansion | Task 7 | ✅ 5 new components, 19 variants |
| 12.7 Delivery Phases | Task 8 | ✅ Verification |